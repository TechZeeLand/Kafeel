## What does this PR do?

<!-- Describe the change and why it's needed. Link the issue it addresses, if any (e.g. "Closes #12"). -->

## How was this tested?

<!-- e.g. "Ran locally via docker compose, verified checkout flow end-to-end" -->

## Checklist

- [ ] `find src -name "*.php" -print0 | xargs -0 -n1 php -l` passes locally
- [ ] README/CONTRIBUTING updated if setup steps, env vars, or the project
      layout changed
- [ ] No secrets, real uploaded photos, or DB dumps included in the diff
- [ ] UI changes match the existing "field ledger" visual theme
